# 2025-11-19 – Projektjournal

## Arbeitsschritte
- Ordnerstruktur verbessert: `code/models/`, `code/evaluation/`, `code/analysis/` erstellt
- Dateiumbenennungen: `rf_baseline.py` → `baseline_model.py`, `rf_pos_models.py` → `position_model.py`, `rf_rank_boost.py` → `moving_average_model.py`
- Batch-Skripte und Dokumentation aktualisiert (Pfade zu neuen Ordnern)
- ROOT-Pfad in allen Python-Skripten korrigiert (`parents[1]` → `parents[2]`)
- Funktionstests erfolgreich (`make_predictions.py --help`, `team_backtest.py --help`)

## Reflexion (kurz)
- Gut: Neue Ordnerstruktur deutlich übersichtlicher, folgt ML-Projekt-Konventionen
- Schwierig: Alle Batch-/Doku-Referenzen systematisch aktualisieren
- Gelernt: Bei Restrukturierungen alle Referenzen durchsuchen und Funktionstests durchführen

## Entscheidung
- Ordnerstruktur: `code/models/`, `code/evaluation/`, `code/analysis/`
- ROOT-Pfad: `parents[2]` statt `parents[1]` wegen Unterordner-Struktur
- Aussagekräftigere Dateinamen: `baseline_model.py` statt `rf_baseline.py`

## Alternativen (kurz)
- `code/ml/` statt `code/models/` → aber "models" ist aussagekräftiger
- `code/tests/` statt `code/evaluation/` → aber "evaluation" passt besser
- Alte Dateinamen beibehalten → aber weniger deskriptiv

## Warum so?
- Übersichtlichkeit: Gruppierung nach Funktion macht Navigation einfacher
- Konsistenz: Folgt Best Practices für ML-Projekte (scikit-learn, Keras, etc.)
- Wartbarkeit: Neue Entwickler finden sich schneller zurecht

## Fehler & Learnings
- ROOT-Pfad-Anpassung: `parents[2]` statt `parents[1]` wegen Unterordner
- Batch-Skript-Testing wichtig nach Umbenennung
- Bei Restrukturierungen: Dateien verschieben → Imports prüfen → Referenzen aktualisieren → Funktionstests

## Nächste Schritte
- Multi-Season Backtests im Frontend fertigstellen
- Full-GW Backtest-Resultate (2022-23 GW2-38) in Maturaarbeit einbauen
